create or replace PACKAGE lm_ssm_tapi_pck
IS
-- Table API package for table lm_segment_status_dom
-- Generated by VI TAPI generator version 6.2  (TAPI generated 03-Jan-2022  05:41:17)

-- Generated global constants
g_con_pack        CONSTANT     VARCHAR2(30) := 'LM_SSM_TAPI_PCK';
g_con_table_name  CONSTANT     VARCHAR2(61) := 'LM_SEGMENT_STATUS_DOM';
g_lock_conflicts  PLS_INTEGER := 0;


FUNCTION ssm_select_row( i_ssm_ID   IN  lm_segment_status_dom.ID%TYPE
                       , o_rt_ssm   OUT lm_segment_status_dom%ROWTYPE )
RETURN BOOLEAN
;

FUNCTION ssm_select_row_upd( i_ssm_ID   IN  lm_segment_status_dom.ID%TYPE
                           , o_rt_ssm   OUT lm_segment_status_dom%ROWTYPE )
RETURN BOOLEAN
;

PROCEDURE ssm_lock_row( i_ssm_ID   IN  lm_segment_status_dom.ID%TYPE )
;

PROCEDURE ssm_lock_row_nowait( i_ssm_ID   IN  lm_segment_status_dom.ID%TYPE )
;


END lm_ssm_tapi_pck;
/




package body lm_ssm_tapi_pck
is




function ssm_select_row( i_ssm_id   in  lm_segment_status_dom.id%type
                       , o_rt_ssm   out lm_segment_status_dom%rowtype )
return boolean
is
   con_proc  constant     varchar2(61) := g_con_pack || '.ssm_select_row';
   l_found                boolean      := false;
begin
   declare
      cursor c_ssm ( i_ssm_id   in  lm_segment_status_dom.id%type )
      is 
           select *
             from lm_segment_status_dom ssm
            where ssm.id = i_ssm_id ;
   begin                                                         
      viu_module.enter_module( i_module => con_proc );

      begin
         open c_ssm( i_ssm_id => i_ssm_id );
         fetch c_ssm into o_rt_ssm;
         l_found := c_ssm%found;
         close c_ssm;
      end;

      viu_module.leave_module( i_module     => con_proc 
                             , i_debug_info => case l_found when true then 'found' else 'notfound' end );

      return l_found;

   exception
      when others
      then
         if c_ssm%isopen
         then
            close c_ssm;
         end if;

         viu_module.leave_module( i_module     => con_proc
                                , i_debug_info => sqlerrm );

         viu_info.raise_info( i_calling_procedure => con_proc );
   end;                                                          
end ssm_select_row;

function ssm_select_row_upd( i_ssm_id   in  lm_segment_status_dom.id%type
                           , o_rt_ssm   out lm_segment_status_dom%rowtype )
return boolean
is
   con_proc  constant     varchar2(61) := g_con_pack || '.ssm_select_row_upd';
   l_found                boolean      := false;

   cursor c_ssm ( i_ssm_id   in  lm_segment_status_dom.id%type )
   is
           select *
             from lm_segment_status_dom ssm
            where ssm.id = i_ssm_id 
         for update;
begin
   viu_module.enter_module( i_module => con_proc );

   
   open c_ssm( i_ssm_id => i_ssm_id );
   fetch c_ssm into o_rt_ssm;
   l_found := c_ssm%found;
   close c_ssm;

   viu_module.leave_module( i_module     => con_proc
                          , i_debug_info => case l_found when true then 'found' else 'notfound' end );

   return l_found;

exception
   when others
   then
      if c_ssm%isopen
      then
         close c_ssm;
      end if;

      viu_module.leave_module( i_module     => con_proc
                             , i_debug_info => sqlerrm );

      viu_info.raise_info( i_calling_procedure => con_proc );

end ssm_select_row_upd;

procedure ssm_lock_row( i_ssm_id   in  lm_segment_status_dom.id%type )
is
   con_proc  constant     varchar2(61) := g_con_pack || '.ssm_lock_row';
begin
  declare
     cursor c_ssm ( i_ssm_id   in  lm_segment_status_dom.id%type )
     is
             select ''
               from lm_segment_status_dom ssm
              where ssm.id = i_ssm_id 
         for update;

   begin                                                         
      viu_module.enter_module(i_module => con_proc);

      begin
         
         if i_ssm_id is not null
         then
            begin
                
                ssm_lock_row_nowait( i_ssm_id => i_ssm_id );
            exception
               when viu_constant.exc_lockfailed
               then
                  
                  g_lock_conflicts := g_lock_conflicts + 1;

                  
                  viu_info.reset_package_variables;

                  
                  open c_ssm( i_ssm_id => i_ssm_id );
                  close c_ssm;
               end;
         end if;
      end;

      viu_module.leave_module( i_module => con_proc );

   exception
      when others
      then
         if c_ssm%isopen
         then
            close c_ssm;
         end if;

         viu_module.leave_module( i_module     => con_proc
                                , i_debug_info => sqlerrm );

         viu_info.raise_info(i_calling_procedure => con_proc );
   end;                                                          
end ssm_lock_row;

procedure ssm_lock_row_nowait( i_ssm_id   in  lm_segment_status_dom.id%type )
is
   con_proc  constant     varchar2(61) := g_con_pack || '.ssm_lock_row_nowait';
begin
   declare
      cursor c_ssm ( i_ssm_id   in  lm_segment_status_dom.id%type )
      is
              select ''
                from lm_segment_status_dom ssm
               where ssm.id = i_ssm_id 
          for update nowait;

   begin                                                         
      viu_module.enter_module(i_module => con_proc);

      begin

         
         if i_ssm_id is not null
         then
            
            open c_ssm( i_ssm_id => i_ssm_id );

            close c_ssm;
         end if;
      end;

      viu_module.leave_module( i_module => con_proc );

   exception
      when others
      then
        if c_ssm%isopen
        then
           close c_ssm;
        end if;

        viu_module.leave_module( i_module     => con_proc
                               , i_debug_info => sqlerrm );

        viu_info.raise_info( i_ifo_nr            => viu_constant.c_ifo_lockfailed
                           , i_calling_procedure => con_proc
                           , i_severity          => viu_info.c_severity_error
                           , i_info_arguments    => g_con_table_name
                           , i_additional_data   =>   'ID = "'||i_ssm_id||'"' );
   end;                                                          
end ssm_lock_row_nowait;


end lm_ssm_tapi_pck;
/